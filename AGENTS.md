# AGENTS.md

> **필수 과제 지침서:** [`docs/assignment-requirements.md`](docs/assignment-requirements.md)
>
> 모든 사람과 AI 에이전트는 계획 수립·코드 작성·수정·검토 전에 위 문서를 처음부터 끝까지 읽어야 한다. Issue나 이 문서의 요약만 보고 과제 요구사항을 판단하지 않는다. 과제 충족 여부와 최종 체크리스트는 해당 문서를 직접 기준으로 검증한다.

## 원본 과제 자료

Codex를 포함한 모든 작업자는 요약 문서와 함께 다음 원본 PDF도 확인할 수 있다.

- [`docs/HW1description_plain-20260910.pdf`](docs/HW1description_plain-20260910.pdf) — 원본 과제 설명 12쪽
- [`docs/HW1강의자료-20260910.pdf`](docs/HW1강의자료-20260910.pdf) — 수업용 해설 자료 16쪽

요약 문서와 원본 PDF가 충돌하면 교수자의 최신 안내를 우선하고, 차이를 확인한 뒤 `docs/assignment-requirements.md`를 갱신한다.

## 목적

이 문서는 이 저장소에서 작업하는 사람과 AI 에이전트가 따라야 할 공통 작업 규칙이다. 과제 요구사항 자체는 `docs/assignment-requirements.md`를 기준으로 판단한다.

## 기준 문서와 우선순위

충돌이 발생하면 다음 순서로 따른다.

1. 교수자의 최신 과제 공지와 추가 설명
2. `docs/assignment-requirements.md`
3. 저장소에서 합의된 통신 규약·공통 자료구조·설정
4. 담당 GitHub Issue와 Pull Request 설명
5. 코드 주석과 개인 작업 메모

요구사항이 불명확하면 필수 조건을 더 엄격하게 만족하는 방향을 선택하고, 임의로 요구사항을 축소하지 않는다.

## 반드시 유지할 과제 조건

- Master Node 1개는 AWS·GCP 등 외부 클라우드에서 실행한다.
- Worker Node 4개는 로컬 환경에서 실행하며 각각 독립 Thread로 동작한다.
- Master↔Worker와 Worker↔Worker 통신은 TCP 소켓으로 구현한다.
- Master가 중복 없는 4자리 16진수 Key와 1~100 정수 Value로 구성된 작업 5,000개를 생성한다.
- Worker Ready Queue의 최대 크기는 10이다.
- 작업 처리는 성공 80%, 실패 20%를 적용한다.
- 실패 작업은 Priority Queue를 거쳐 다른 Worker에 우선 재할당하며 성공할 때까지 반복한다.
- 예상 대기시간은 `남은 작업 수 × 2초`이고, 15초를 초과하면 P2P 부하 분산을 수행한다.
- P2P 이전 작업 수는 1~3개이며, ACK를 받은 뒤 송신 Queue에서 제거한다.
- System Clock은 OS 시간이 아닌 Thread-safe한 전역 논리 시계다.
- 작업 처리에는 1~3초, 통신에는 1초의 논리 시간을 반영한다.
- 논리 시간 구현을 위해 실제 `sleep()`으로 기다리지 않는다.
- 로그 형식은 `[clock] NODE | EVENT | STATUS | message`를 사용한다.
- STATUS는 `INFO`, `SUCCESS`, `FAIL`, `WARN`만 사용한다.
- 5,000개 작업이 모두 성공한 뒤 통계 출력, 로그 저장, 소켓·Thread 정리 순으로 정상 종료한다.
- 시연 영상에는 목소리 설명이 필수이며, 자막은 필요하지 않다.

## 공통 설계 규칙

- TCP 메시지는 하나의 공통 프로토콜 모듈에서 직렬화·역직렬화한다.
- TCP 스트림의 메시지 경계를 명시적으로 처리한다. 한 번의 `recv()`가 메시지 한 개와 같다고 가정하지 않는다.
- 메시지에는 최소한 메시지 종류, 송신자, 작업 또는 요청 식별자, 필요한 payload가 포함되어야 한다.
- REGISTER, TASK, QUEUE_STATUS, RESULT_SUCCESS, RESULT_FAIL, P2P_STATUS_REQUEST, P2P_STATUS_RESPONSE, P2P_TRANSFER, ACK, TERMINATE 흐름을 모두 표현할 수 있어야 한다.
- Task, Message, WorkerStatus 등 공통 자료구조를 담당 모듈마다 중복 정의하지 않는다.
- IP, 포트, 경로, 비밀키는 코드에 고정하지 않고 명령행 인자·환경변수·설정 파일로 주입한다.
- 공유 Queue, Clock, 통계, 로그에는 Lock이나 thread-safe 자료구조를 사용한다.
- 네트워크 오류, 잘못된 메시지, Queue overflow, 연결 종료가 발생해도 전체 프로그램이 무응답 상태에 빠지지 않게 한다.
- 동일한 작업이 재전송되거나 ACK가 지연되는 경우를 고려하여 작업 ID를 기준으로 추적한다.
- 담당 기능 때문에 공통 규약을 바꿔야 하면 관련 코드 전체를 독단적으로 수정하지 말고 PR 설명에 변경 이유와 영향을 기록한다.

## 작업 절차

1. 작업 전에 `docs/assignment-requirements.md`, 이 문서, 담당 Issue를 모두 읽는다.
2. 담당 기능에 맞는 브랜치에서 작업한다.
3. 기존 공통 인터페이스와 메시지 규격을 먼저 확인한다.
4. 담당 범위의 구현과 최소 테스트를 함께 작성한다.
5. 문법·실행·테스트를 로컬에서 확인한다.
6. 변경한 동작, 실행 방법, 확인 결과, 남은 문제를 PR에 작성한다.
7. 최종 통합 과정에서 조장이 코드를 수정·재구성할 수 있음을 전제로 한다.

## 코드 수준과 주석 언어

- 코드는 컴퓨터공학과 3학년 학부생이 직접 이해하고, 실행 흐름을 설명하며, 오류를 수정할 수 있는 수준으로 작성한다.
- 과제 요구사항을 정확히 만족하는 범위에서 구조를 단순하고 명시적으로 유지한다.
- 불필요한 엔터프라이즈 패턴, 과도한 추상화, 메타프로그래밍, 이해하기 어려운 축약 표현, 과제와 무관한 대형 의존성을 도입하지 않는다.
- 수준을 맞춘다는 이유로 동시성 안전성, TCP 메시지 경계 처리, 오류 처리, 테스트 또는 과제 필수 기능을 생략하지 않는다.
- 식별자(클래스·함수·변수 이름)는 일반적인 영문 명명 규칙을 사용한다.
- 코드 주석과 설명 주석은 한글로 작성한다.
- 주석은 코드가 그대로 보여주는 내용을 반복하지 말고, 동시성 처리 이유, 통신 규약, 예외 상황, 알고리즘 선택 이유처럼 이해가 필요한 부분을 설명한다.
- 외부 라이브러리의 고유 명칭, 프로토콜 메시지 이름, 로그 STATUS·EVENT 등 과제에서 정한 영문 용어는 원문 표기를 유지할 수 있다.

## 코드 품질 규칙

- 기능을 작고 명확한 모듈과 함수로 나눈다.
- 중복 코드와 전역 mutable state를 최소화한다.
- 예외를 빈 `except`로 삼키지 않는다.
- 무한 대기나 무한 재시도에는 종료 조건 또는 정상 종료 신호 처리를 둔다.
- 테스트에서 재현이 필요하면 난수 seed를 주입할 수 있게 설계하되 실제 실행의 무작위 동작은 유지한다.
- 로그와 통계 수치가 실제 상태 변화와 같은 지점에서 갱신되게 한다.
- 제출 요구사항과 무관한 프레임워크나 대형 의존성을 임의로 추가하지 않는다.
- 생성형 도구로 만든 코드는 그대로 제출하지 않고 실행 흐름, 동시성, 예외 처리, 통신 경계를 직접 검토한다.
- 개인 토큰, 클라우드 키, 공인 IP, 비밀번호, 로컬 절대경로를 커밋하지 않는다.

## Git과 PR 규칙

- 기능 브랜치는 `feat/<담당>-<기능>`, 수정 브랜치는 `fix/<기능>` 형태를 권장한다.
- 담당 범위와 무관한 대규모 포맷 변경이나 파일 이동은 피한다.
- 커밋은 하나의 논리적 변경 단위로 작성한다.
- 생성물, 캐시, 가상환경, 비밀 설정은 커밋하지 않는다.
- 최종 제출에 필요한 `Master.txt`, `Worker1.txt`~`Worker4.txt`, `AllDefinedLogs.txt`, `download.txt`, `Readme.txt`는 일반 `*.txt` 무시 규칙을 만들지 않고 별도로 관리한다.
- 조장은 통합과 과제 충족을 위해 모든 브랜치의 코드를 수정하거나 재구성할 수 있다.

## 담당 작업 완료 기준

담당 기능은 다음 조건을 모두 만족해야 완료로 본다.

- 담당 Issue의 체크 항목이 구현되어 있다.
- 최소 정상 흐름과 주요 실패 흐름을 직접 실행하거나 테스트했다.
- 하드코딩된 개인 환경 정보와 비밀정보가 없다.
- 공통 프로토콜과 자료구조를 중복 또는 임의 변경하지 않았다.
- 실행 방법과 검증 결과가 PR 설명에 기록되어 있다.
- 미완성 항목, 알려진 오류, 통합 시 주의사항을 숨기지 않고 명시했다.

## 최종 검수

자동 테스트나 AI 검토만으로 제출 완료를 선언하지 않는다. 최종 제출 전에는 조장이 `docs/assignment-requirements.md`의 전체 체크리스트를 기준으로 실제 클라우드 Master, Worker 4개, P2P 통신, 장애 재할당, 로그·통계, 영상 링크, ZIP 압축 해제와 재실행을 직접 검수한다.
